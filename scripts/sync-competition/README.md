# Competition Sync

This directory is reserved for tooling around the Hoops Scorebook integration.

Planned utilities:

1. Fixture capture from public API responses
2. Contract checks against `openapi.json`
3. Manual revalidation helpers for event tags
